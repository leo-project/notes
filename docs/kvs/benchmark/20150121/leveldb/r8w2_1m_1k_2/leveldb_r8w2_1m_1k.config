{mode, max}.

{duration, 3}.

{concurrent, 1}.

{driver, basho_bench_driver_leveldb}.

{key_generator, {int_to_bin_bigendian,{uniform_int, 1000000}}}.

{value_generator, {fixed_bin, 1000}}.

{operations, [{get, 8}, {put, 2}]}.

{code_paths, ["../eleveldb"]}.

{leveldb_dir, "/tmp/eleveldb.bench"}.

{leveldb_db_options, [{create_if_missing, true}, {write_buffer_size, 16777216}, {total_memory, 8589934592}]}.

